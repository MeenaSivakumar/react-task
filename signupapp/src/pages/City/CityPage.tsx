import { CityForm } from "../../modules/CityForm/CityForm";
import { EmailForm } from "../../modules/EmailForm";

export const CityPage = ({}) => {
  return (
    <div>
      <CityForm/>
    </div>
  );
};
